# Tuples : A tuple is a immutable data type in python. (Constant list)
# Immutable : Cannot update the values.

#creating a tuple
t = (1, 2, 4, 5)
t1 = () #empty touple
t2 = (1,) #tuple with single element, can't write as (1)

#printing the elements of tuple
print(t[1])
print(t1)