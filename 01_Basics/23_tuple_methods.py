# Tuple Methods
t1 = (1, 2, 3, 4, 5, 4, 8, 4, 5, 6, 9, 3, 2, 1, 0, 6)

#1. count(element) : returns the number of occurance of the given element
print("Count of 4 : ", t1.count(4))

#2. index(element) : search the index of the given element
print("Index of 2 : ", t1.index(2))
